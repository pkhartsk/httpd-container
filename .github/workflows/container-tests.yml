on:
  issue_comment:
    types:
      - created
jobs:
  container-tests:
    uses: "pkhartsk/.github/.github/workflows/container-tests.yml@main"
    with:
      versions: "['2.4', '2.4-micro']"
      openshift-versions: "['2.4']"
    secrets: inherit
